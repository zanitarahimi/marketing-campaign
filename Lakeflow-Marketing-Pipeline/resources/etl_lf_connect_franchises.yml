# yaml-language-server: $schema=../bundle_config_schema.json

resources: 
  pipelines:
    lf-connect-campaigns:
      name: lf-connect-campaigns
      root_path: ../src/lf-connect-campaigns/
      libraries:
        - glob:
            include: ../src/lf-connect-campaigns/transformations/**
      serverless: true
      development: false
      catalog: ${var.catalog_name}
      schema: ${var.schema_name}
      photon: true
      channel: PREVIEW